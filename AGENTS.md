# 项目协作规范

## 会话入口

- 开始工作前阅读 `openspec/project.md`、`docs/0_status/README.md` 和 `docs/HANDOFF.md`。
- 稳定架构与系统不变量以 `openspec/` 为准；当前任务、实验结论与接手顺序以 `docs/` 为准。
- 工作区可能包含用户或其他会话的未提交改动。先检查状态，不得擅自重置、覆盖或清理。

## 命令与验证

- 所有命令从仓库根目录执行。
- Python 代码通过模块入口运行：`python -m <模块>`，不要直接运行源码路径。
- 测试范围与风险匹配。常规 Python 测试可使用 `python -m pytest -n 4 ...`；Go 引擎使用 `go test ./gicg_engine/tests/ -count=1` 和 `go build ./...`。
- `smoke_full` 只在大版本、配置协议或网络架构变化时运行；普通测试默认不包含该层级。
- 提交前启用 `git config core.hooksPath .githooks`，并通过格式、行数与 OpenSpec 索引检查。阈值以检查工具和 OpenSpec 为唯一事实来源。

## 训练与远端操作

- 正式训练统一通过 `python -m tools.runs.train <配置>` 启动；查看、恢复、标记、同步和拉取使用对应的 `tools.runs.*` 命令。
- 远端训练和诊断只使用 `tools.runs.*` 封装。禁止手写 `ssh`、`scp` 或 `tar | ssh` 绕过项目流程，除非与项目逻辑无关。
- 设备授权状态属于会话状态，以用户最新指令为准。

## 实验产物

- `artifacts/` 的顶层目录必须是实验系列的 `experiment_tag`。
- 同一系列的 seed、重试、训练阶段和评估必须归入同一个 tag；`run_label` 只描述单次运行。
- 正式运行目录使用 `artifacts/<experiment_tag>/<时间戳>_<六位序号>/`，并保存自包含的配置、metadata、checkpoint 和指标。
- 实验工具不得随意在 `artifacts/` 根目录创建目录或文件，也不得重复复制已有 checkpoint。

## 文档归属

- 系统当前是什么、必须满足什么，写入 `openspec/`。
- 当前状态、实验过程、结果、复盘和历史记录，写入 `docs/`。
- 开发命令与 agent 协作约束只写在本文件，不复制架构规范或实验状态。

# 通用规范

## 交付物文案

用户可见的界面、PDF、PPT、报告、截图和导出文件只服务产品或业务目标，必须像真实产品内容。

- 不把 agent 的思考过程、实现理由、调试过程或下一步计划写进交付物。
- UI 中禁止出现“我将”“我们可以”“本页面用于展示”“这里会”“用于说明”“实现了”等解释型文案，除非它本就是帮助或空状态文案。
- 实现思路、验证方式、限制与取舍只写在聊天回复、代码注释、提交说明、文档或计划文件中。
- 只有用户明确要求展示设计说明、方法论、实现过程或工作记录时才例外。
- 代码不需要过度产品化、工业化，这里的描述仅用于产出结果的文案、注释等内容。

## 无痕改动

- 除非用户特别需求或与常理偏差较大，否则不添加解释改动原因的注释、标记或临时说明。
- 代码应直接呈现最终结构，不保留临时说明或迁移标记。

## 提交流程

- 提交信息必须准确描述最终交付结果，不描述中间过程。

## 减少测试，直接失败

- 任何非预期的输入，在代码逻辑中都应该直接失败并导致中止程序，不要做兼容。
- 同理，对输入的测试都是冗余的，不要提前加测试。
- 除了状态管理、同步等完全代码内部逻辑外，尽量减少测试。
- 测试只应该断言行为的结果，不应该断言任何内部实现。

