# Don't ship local artifacts / venvs into image build context.
.venv
artifacts
.git
.githooks
__pycache__
*.pyc
*.pyo
.pytest_cache
.ruff_cache
.DS_Store

# Keep the existing macOS .dylib out — container builds its own .so.
gicg_env/libgicg.dylib

# IDE / editor
.vscode
.idea
*.swp

# Docs are not used at runtime
docs

# CI / tooling cache
node_modules
