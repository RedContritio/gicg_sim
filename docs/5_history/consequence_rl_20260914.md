# 冻结预测器残差RL：本轮更新幅度过小

2026-09-14，56 `D:/gicg_consequence_20260914/artifacts/consequence_rl_20260914` 已 complete，3456.28秒（57.60分钟），等待器12906正常退出。[固定协议](cards/consequence_policy_rl.md)。

两臂各4×512局，16workers，冻结backbone/预测头，仅训练零初始化残差；候选使用预测值，对照对应输入为0。终局奖励，lr1e-5，温度0.5，master95800。

## 强度与响应

95900变体开发220局/候选：

| 分支 | 初始 | 第1轮 | 第2轮 | 第3轮 | 第4轮 |
|---|---:|---:|---:|---:|---:|
| 候选 | 19.09% | 17.27% | 17.27% | 16.36% | 17.27% |
| 对照 | 19.09% | 17.27% | 17.27% | 17.27% | 16.36% |

两组均选择初始模型。96000独立440局，初始和所选模型得分20.91%，场景聚类95%区间[15.91%,25.92%]。候选差值0是初始对自身；对照初始权重相同且残差0，行为亦相同。不能把这些0差值理解为“训练前后等效”的证明。未独立复测最终轮强度。

初始/候选最终/对照最终的凯亚16例均为预测排名16/16、技能评分排名16/16、全动作选择较高伤害技能0/16，第一步仍选一掷乾坤。此探针不证明对局最优行动应为直接伤害。

## 更新幅度

候选4轮更新数1362/1358/1380/1334，对照1362/1358/1382/1334，均无KL早停。最后一轮平均anchor KL候选2.81e-5、对照2.74e-5；sample KL约1e-6。说明本预算下策略概率变化很小，不能根据两组接近就认定预测特征无用。训练采集分数约13.7%–15.2%，无上升趋势。

下一步先从本轮实际轨迹校准残差学习率，比较更新后的独立轨迹KL、概率变化和响应，而非再用同样学习率重复长程对照。RL入口已新增正有限 learning_rate 配置，默认仍1e-5并写入resume settings；实验新学习率尚未选定。也需检查预热到配对预测训练是否损害了原策略，当前没有同面板前后数据，不能先下因果结论。

目标未达成；最终三组保留验收种子未使用。原始completion、比较报告、三份探针和两臂result均已回传本机同名artifacts目录。

## 校准学习率后的结果

`artifacts/consequence_calibrated_rl_20260914` complete，3607.04秒，lr=0.001，其余每臂4×512局/16 workers。master96200，原生开发96290，变体选模96300，独立复测96400。等待器34391正常退出，报告已回传。两臂均无KL早停；最后一轮anchor KL候选0.00867、对照0.01231，更新已明显发生。

变体开发初始21.82%；候选四轮22.73/26.36/22.73/23.64%，选择第2轮；对照22.73/25.45/23.64/26.36%，选择第4轮。独立110场景×两席位×两布局=440局/模型：

| 模型 | 胜率 | 场景聚类95%区间 | 相对初始差值95%区间 |
|---|---:|---:|---:|
| 初始 | 19.09% | [14.09%,24.09%] | — |
| 候选第2轮 | 21.36% | [16.82%,26.36%] | [-1.36,5.91]个百分点 |
| 对照第4轮 | 24.09% | [18.64%,30.00%] | [0.45,9.55]个百分点 |

候选相对对照-2.73个百分点，配对95%区间[-6.36,0.91]个百分点。此单次训练显示对照相对初始有收益，不能证明后果预测提供额外收益，也不能证明两方法存在确定差异。最终三种子验收仍未运行。

初始及两臂最终16例探针仍为预测16/16、技能排名16/16、完整行动选择较高伤害技能0/16。候选所选为中间第2轮，不能用最终探针代替所选模型证据。下一步补该探针，并在同面板检查监督预热到配对训练的策略变化，区分初始化退化与RL学习不足，再确定长预算分配。当前没有证明稳定胜过D2。

## 暂停前补充诊断

`artifacts/consequence_followup_20260914` complete，103.45秒。所选候选第2轮和对照第4轮探针均预测16/16、技能排名16/16、全行动选择伤害领先技能0/16。补齐了中间候选的探针证据。

同一96400面板/440局，监督预热模型34.55%，配对规则学习后的初始模型19.09%；配对差值-15.45个百分点，95%区间[-22.73,-7.73]个百分点。此单次初始化的阶段对比说明配对训练阶段伴随明显策略退化，不能推广为规则监督必然有害。预测改善和策略退化可以同时发生。下一步应优先保留预热策略、隔离预测训练的参数影响，并用同面板/规则响应验证，再分配RL预算。

用户要求暂停；训练/评估均正常结束，未启动下一实验。必要文件与恢复说明见[暂停交接](../HANDOFF_PAUSED.md)。
