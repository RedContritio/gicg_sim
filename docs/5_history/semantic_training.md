# 语义规则图网络：D1 与 shuffle 验收

2026-09-13，北京时间。持续目标为当前任务对 D1 成绩不低于50%，对shuffle不敏感。
本次冻结模型通过已预先落盘的成绩/布局验收；方法是D2监督模仿，未进行后续RL更新。
因此这里只完成当前任务的策略成绩与鲁棒性里程碑，不宣称纯RL成功或新卡零样本成功。

## 方法与任务范围

- 当前配置 `configs/dmc/semantic_goal.toml`，环境与原 remote_resume_v17 相同：赤蝶对墨客，各1角色，8回合上限，测试增幅/碎片卡及填充牌，遮蔽敌方骰子，全部shuffle开启。
- 规则图连接counter/card与公开hook语义；保留全部活动counter数值，buff来源对应语义counter。模型输入不调用export_view；验收工具仅用它核对物理状态。
- 新模型随机初始化，未继承旧权重。128局D2对D1完整示范，对双方角色交替采样，共2445条D2决策。按D2并列最佳动作均匀交叉熵训练2000步，batch32，AdamW lr0.0003。
- 数据种子123000、网络初始化123001、minibatch种子123002。没有根据最终验收调参或选择检查点，固定用第2000步权重。
- 推理仅使用神经网络，Q头输出在本次交叉熵训练后应理解为策略logit，不是校准的回报预测；argmax前以1e-5精度取整缓解浮点近似平局。

## 独立评估

| 集合 | 种子 | 物理场景×席位×布局 | 胜率/成绩 | 按场景聚类95%区间 |
|---|---:|---:|---:|---:|
| 开发 |124000|64×2×4=512局|85.9375%|79.6875%–91.40625%|
| 最终验收 |125000|256×2×4=2048局|81.640625%|78.3203125%–84.9609375%|

最终验收1672胜、376负、0平；四布局各自成绩均81.640625%。
Bootstrap以物理场景为cluster，保留同场景双席位与四布局相关性，10000次采样。
各布局相对布局0的配对成绩差均0，区间[0,0]；512组双席位轨迹均在四布局产生相同逻辑动作序列。
轨迹哈希仅含逻辑动作，支付一致性单独由下述同状态验收确认。

另用seed126000、64个场景×双席位，在每个模型决策物理状态同时核对4布局：

- 2379个决策状态，7137次布局对比；状态视图、完整合法动作身份及支付候选均先核对相同。
- 逻辑动作一致7137/7137，含支付一致7137/7137，均100%。
- 138852个合法动作logit比较，平均绝对差1.1668e-6，最大绝对差1.1444e-5。
- 所有对局正常终局。有限样本与当前池通过，不等于任意新卡/任意排列的数学保证。

## 56执行与留档

用户授权大规模计算优先56并可提高并行。新源码在 `D:/gicg_goal`，旧环境在 `D:/gicg_dev`。
新目录Windows junction不可遍历，已移除链接；使用 `D:/gicg_dev/.venv/Scripts/python.exe`，cwd为新目录。

- 8进程采集+GPU训练总计125.5秒，稳态约17更新/秒；观测GPU67%、显存5570MiB。
- 开发评估16进程约9.6秒，正式2048局16进程约33.3秒（同时运行8进程同状态检查）。
- 所有本轮训练/验收进程正常退出。
- 本机及56：`artifacts/202609121729_000001_semantic_warmup/ckpts/latest.pt`。
- 同目录metadata、解析配置；本机额外 `source.tar.gz` / `source_manifest.json` 冻结源码（含实验模型与工具，弥补生产指纹不覆盖tools）。
- checkpoint格式 `semantic-q/1.0.0`，保存优化器、CPU/CUDA/Python RNG、形状；当前尚无继续训练CLI。
- 权重SHA256：`42d97aad4de31f2ab3f7845e50b6d418e4c3bd6a1642acf52898a836116c2d8a`。
- 生产指纹：`b3e84a0a399cb7e0958f540917abb2c473c72ca7aa7fa4b96eab65d4adfb15a2`；本机严格加载与SHA校验通过。
- 原始结果已拉回：`artifacts/semantic_warmup/result.json`、`artifacts/semantic_warmup_eval/result.json`、`artifacts/semantic_acceptance/result.json`、`artifacts/semantic_acceptance_stability/result.json`。

验证：本机真实对局语义网络/图接口/原buff学习回归共12测试通过；56真实对局重排与梯度测试通过；新增语义工具ruff检查通过。
下一阶段若继续RL，应以这份冻结模型为基线，补恢复入口并区分策略logit与回报Q初始化；训练/开发使用新种子，不能反复使用本次验收集选模型。
