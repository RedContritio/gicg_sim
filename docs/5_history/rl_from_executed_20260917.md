# 从 executed-warmup 出发的 RL（2026-09-17 深夜）

协议卡 [rl_from_executed_warmup](../3_plans/cards/rl_from_executed_warmup.md)。
单变量对照 run 000045：同算法（8×128、F1-D2 对手/开发、lr 1e-5、value
baseline、T=0.5、anchor KL 0.02、50% 变体），只换初始（warmup3，run 000051）
与 seed（96200/dev 96290）。指纹 `afa1cdcd…`。

## RL 轨迹（dev 96290，55 场景原生 vs F1-D2，220 局/轮）

35.45 → 40.00 → **42.73** → 40.91 → 38.18 → 40.00 → 40.91 → **43.64**（iter8）。
采集分同步上升（0.31→0.36~0.41 波动）——与后果残差 RL 的完全平坦形成鲜明对比：
**executed 初始化上的全网络 RL 是有效的**。

## 独立确认面板（run 000053 iter8）

| 面板（110 场景 × 双方 × 2 布局） | RL iter8 | warmup3 | 配对差 |
|---|---|---|---|
| 原生 vs F1-D2（96210） | 41.82% [35.5, 48.2] | 40.45% [34.1, 46.8] | +1.36pp [-6.4, +9.1] |
| heldout 变体 vs F1-D2（96220） | **43.18% [37.3, 49.1]** | 35.91% [30.0, 41.8] | **+7.27pp [0.0, +14.1]** |

- 原生配对跨 0（RL 原生增益未证实），变体配对点估计 +7.3pp、区间触 0——
  RL 的收益目前主要体现在变体规则下；零样本泛化保持（变体 ≥ 原生）。
- 距「稳定战胜 D2」（CI 下界 > 50%）仍差约 13–15pp；最好面板 CI 上界 49.1%
  已逼近 50%。

## 09-17 深夜全战役小结（vs F1-D2）

| 阶段 | 最好得分（vs F1-D2） |
|---|---|
| 上午：uniform warmup / full+RL | 32.7% / 27.7% |
| executed 目标（warmup3） | 40–41% |
| + RL（run 000053 iter8） | **41.8% 原生 / 43.2% heldout 变体** |

## RL 预算翻倍（resume 至 16 轮，run 000055）

dev 9–16：39.1/40.0/37.3/41.8/40.0/40.0/38.2/41.8——平台期，未超 iter8 的 43.6%。
独立确认（新 seed 配对）：

| 面板 | RL16（000055） | RL8（000053） | 配对差 |
|---|---|---|---|
| 原生 96230 | 42.27% [36.4, 48.2] | 37.73% [32.3, 43.2] | +4.55pp [-1.4, +10.0] |
| heldout 变体 96240 | 40.45% [35.0, 46.4] | 37.27% [31.4, 43.2] | +3.18pp [-2.3, +9.1] |

点估计均为正但区间跨 0：**预算翻倍收益未证实**（与数据缩放同模式——小样本
面板下 ±5pp 噪声使 3~5pp 增益难以确权）。当前最好模型 = RL16（run 000055）。
种子追加：96230/96240。

## 状态与下一步

当前最好模型 RL16（run 000055）：原生 42.3% [36.4,48.2]、heldout 变体
40.5% [35.0,46.4] vs F1-D2；距 CI 下界 >50% 仍差约 14pp。

- ~~RL 预算翻倍~~ 已完成：收益未证实（见上）。
- 下一假设：**DAgger×executed**——DAgger 09-14 的证伪是在 uniform 目标下
  （当时目标缺陷是混淆变量）；executed 标签 + 聚合 replay 直接攻击复合误差。
- 产物根：`artifacts/rl_from_executed_20260917/`（56）。
- 种子：96200/96290（RL）、96210（原生确认）、96220（变体确认）、96230/96240
  （RL16 vs RL8）；96280（rollout 搜索）、96300/96390（去锚点臂）、96400/96401
  （value head 专训）、96410/96430（d128×executed）。
