# 2026-09-12 提交整理

> **历史工作区快照**：下列分支、HEAD、文件数和阻碍只描述
> 2026-09-12 的提交整理，不能用于判断当前 Git 状态。

当前分支 `i33-roundend-actor-ctx`，HEAD `cdc3f8b`。
检查开始时有 407 条 Git 状态记录；展开新增目录后共 567 个实际文件。
初检时暂存区为空。现已提交1506454并通过a4b59b8合入dev；未配置remote，无推送操作。

## 提交范围

| 范围 | 文件数 | 主要内容 |
|---|---:|---|
| 引擎、卡牌、环境与消费者 | 226 | 自定义规则、buff 实例和顺序、暂停恢复、公开观测、MCTS/actor 接口 |
| 训练与配置 | 122 | NN 表达、动作编码、完整恢复、每局 shuffle、初训配置和测试 |
| 工具 | 48 | 规则验收、基线梯度、技能滥用者、成对评估、清理工具 |
| OpenSpec 与文档 | 171 | 规则确认、阶段验收、来源清单、实验记录和交接 |

初步建议按依赖拆分，不能只按目录机械分组：

1. 规则引擎、观测、NN 与训练恢复构成一个可运行的基础提交，包含对应测试和规范。
2. 评估梯度、技能滥用者与实验工具单独提交，依赖基础提交。
3. 实验结果和当前交接最后提交；历史 manifest 保留原哈希和历史含义。

这些是候选边界，尚未逐文件暂存或验证中间提交。
`artifacts/` 下权重、经验池、远端临时执行脚本和原始日志继续作为运行产物；
需要复用的远端续跑/停止流程，应先提炼进正式工具再纳入版本控制。

## 整理初检（修复前的历史结果）

- `git diff --check`、改动 Python 的 Ruff 格式检查（176 文件）、Go 格式检查、OpenSpec 索引通过。
- v17 来源清单 1170 文件全部 MATCH。
- 训练源码指纹仍为 `8bc1dce77cd90f8eaf7f71be117bdf532df1da036bd8f62123b79521bf1a8dc5`。
- `go test ./...` 在正常本机权限下通过；沙箱首轮因缓存、共享内存和回环端口限制失败。
- Python 正常权限回归在 131.79 秒后主动中断：1294 passed、6 skipped、5 failed；剩余测试未完成，不是全套通过。
  五项失败来自 `training/paradigms/dmc/collector.py:246`，异步路径引用未定义的 `seeds`（NameError）。
  涉及四项 `test_dmc_async_collector.py` 测试和 `test_dmc_mp_factories_end_to_end`。
  当前训练采用 serial；该失败本身不证明 serial 训练受污染。尚未修改生产源码。
- 全仓 35 个行数超限文件，本次涉及 11 个，全部在 HEAD 中已经超限。
  CLAUDE.md 描述历史超限沿用，但当前 hook 实际会检查所有暂存文件，没有历史豁免逻辑。
  不能声称普通提交钩子已经通过；需先明确并实现历史超限的处理方式。
- 仅为格式或行数拆分生产文件也会改变训练来源指纹，当前检查点将被严格加载器拒绝。
  本次整理未重构这些文件，也未放宽检查点来源验证。

详细文件清单和旧/新行数比较位于 `artifacts/commit_review_20260912/`。
当前验收日志保存在本机 `/tmp/gicg-submit-*.log`，整理完成后复制至上述产物目录。

## 后续修复

用户已确认正常重构，两个初检阻碍现已修复：
异步统计取 EpisodeRecord.scenario_seed；11 个超限文件按职责拆分，所有改动文件通过行数检查。
原始训练源码已归档，未放宽来源校验，远端旧环境不变。
完整验证与新来源清单见 [v0.1.0 验收](../../openspec/changes/submission-refactor/results.md)。
用户随后要求全部提交：实际采用一个完整基础提交1506454，避免中间接口不一致；
通过a4b59b8合入dev。旧分支清理记录见../5_history/branch_cleanup/README.md。
