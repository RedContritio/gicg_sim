# 五角色 2v2 全 L1–L6 训练目标

2026-09-13 用户目标：按难度梯度逐渐充实训练卡组，最终 L6 及以下所有卡牌加入后，仍稳定胜过 D1，并对 D2 有优势。计算、示范采集和对局评估放在 56。

## 最终环境

以 data/pools/v_legacy/cards/L1–L6 下实际 26 种卡为覆盖全集。角色仍为赤蝶、墨客、猫咪、刻师傅、天星，双方随机各两名且不重复；10 回合裁决保持。
用户确认最终固定 30 张、随机合法构筑，每种最多两张。天赋和武器按每局实际队伍适用性筛选，使用引擎产生 eligible 集，不能在 Python 另写角色/武器规则。从每种两份的物理卡集合无放回采样 30 张；双方分别抽样，评估时构筑与骰子/布局种子分离。早期不足 15 种适用牌时保留较小牌组，不用无效果牌超量填充。

最终所有 26 种牌须同时在候选池中；单局无需同时抽到全部牌。要统计每张牌按队伍的入组、抽到、可用、使用次数，不能以“配置声明过”代替实际覆盖。角色生成牌作为规则依赖加载，不冒充起始牌组卡。

## 课程顺序

1. 已批准的八种牌×两张、16张固定牌组：独立 BC 与 RL 基线，诊断扩池损失。
2. 在现有基础补齐 L1–L3；保留已引入的玄冰/乘胜追击，适用牌不足 15 种的队伍用较小牌组。
3. 补齐 L4，启用固定 30 张随机合法构筑（先核验所有队伍容量）。
4. 补齐 L5 五种角色天赋；逐角色验证加载、出牌目标及触发效果。
5. 补齐 L6 以攻代守、以逸待劳，形成完整 26 种池。

速速茶点/以逸待劳此前为留出适应牌；在其课程阶段加入前做一次冻结模型适应前测，加入后明确标记已参与训练，不能继续声称未见新卡泛化。当前两个 demo 卡可以在过渡期保留，最终验收至少覆盖原有 26 种，不用 demo 替代原牌。

## 训练和晋级

D2 为强度主标准，D1 为基础与退步检查，不运行 D3。当前 16 张试验使用 512 局 D2 示范、4000 BC 步、8×128 RL，只是扩池首轮预算，不是最终能力上限。若不达标，先看新牌动作/支付/目标、监督拟合与终局失败模式，再增加示范覆盖/训练预算或 D2 训练对手。

只在开发集上调参选轮次；独立测试集不参与后续选择。每阶段保存牌池、构筑策略、源码、配置、种子、BC/候选/恢复权重和逐局结果。中间阶段不必为短期分数永久排除难牌；最终目标不可缩成八种牌、只赢 D1 或只证明引擎可运行。

最终验收预先冻结配置和选模方法，至少 3 个独立 RL 训练种子，各自仅以开发集选模；每个最终候选使用新场景/构筑种子，15 组队伍配对等量、双席位，布局重复只作一致性检查。每个候选对 D1 的场景聚类 95% 区间下界须超过 50%；对 D2 也要求独立面板区间下界超过 50%，并报告训练种子差异和跨种子汇总，不能只挑最好一个种子。另报告超时裁决、击倒胜局、分队伍、卡牌覆盖与动作/支付跨布局稳定性。仅一个模型跨场景稳定不等于训练过程稳定。

## 当前证据

当前远端主进程4817活跃，L3、L4、L5已完成，L6已进入1024局示范采集，随后8000步BC和12轮RL。完整课程控制状态以56的 `artifacts/full_pool_curriculum/result.json` 为准。L3/L4开发成绩及迁移记录见文末。随机合法构筑已接入实际训练工厂，33项相关回归、56四阶段64局真实预检均通过。真实引擎构筑检查覆盖10种己方双人队伍、40组抽样构筑，全部30张、每种不超过两张，26种牌均被抽到；另测试构筑不随布局改变、reset保持构筑。

逐卡审计工具 tools.experiments.semantic_training.card_coverage 已实现并通过真实D2对局测试，按队伍记录起始牌组副本/入组局数、进入手牌局数、合法机会局数、使用局数和出牌事件数。入组/见过/可用按局计，使用另计次数；不把多次查看同一张手牌冒充多次抽牌。配置中的无效果牌不使用可能是合理策略，不能把每张牌都强制打出作为能力门槛。56已完成240场景双席位共480局D2自对战参照，输出 artifacts/full_pool_teacher_coverage.json 已拉回并验证全部牌组30张/最多两份、26种牌均入组且进入手牌；句柄76984正常退出。刺刺猫爪/发现静电/蝶鳞/以逸待劳使用局数为0，守正1、星愿2，说明D2监督对天赋/以逸待劳的实际教学很少，不等于这些牌没有价值。候选冻结后需用同工具审计并比较。

## 自动课程入口（2026-09-13）

随机构筑已接入 training/core/env_factory.py，通过配置 random_deck_size=30启用。configs/dmc/curriculum_l3/l4/l5/l6.toml 逐级补齐原26张牌（过渡期额外保留两个demo）；训练/评估使用同一构筑代码，布局种子不影响构筑。
入口 tools.experiments.semantic_training.curriculum：每阶段1024局新D2示范、8000步BC（下一阶段从开发集较优候选迁移）、12×256局对D2的RL；D2开发120场景双侧双布局。最终从完整池迁移模型出发，271000/281000/291000三个独立RL种子各24×256局，分别240场景开发选模；全部冻结后才运行每个种子720场景×双侧×2布局对D1/D2独立面板及四布局稳定性。程序会区分实验完成与 strength_gate_passed，不达标继续改进，不能标目标完成。
控制目录 artifacts/full_pool_curriculum，result.json 保存当前阶段、预算、每阶段结果和最终候选；源码与配置归档。56正式启动前先以 tools.experiments.semantic_training.preflight_curriculum 每阶段16局做真实D2对局与NN观察预检。

56四阶段共64局预检已全部正常结束（各阶段监督记录615/656/655/621）；随后完整课程stream句柄4817已启动，超时上限86400秒。预算预计数小时，尚未完成或证明强度目标。

验收工具 verify_curriculum.py 已准备，5项反例测试验证能拒绝候选SHA变化、缺失/重复对局、布局不同结果及被改写的汇总胜率；最终逐局数据重新按720物理场景聚类计算区间。工具还核验三个训练种子、15组对阵均衡和四布局稳定性，逐卡使用审计及最终报告仍需另行完成。当前主进程4817活跃，L3 BC已到7600/8000步。

L3 BC run000015 已完成1024局示范和8000更新（1120.5秒）；D2开发120场景双侧双布局得分40.83%，95%[35.42,46.25]，未达优势，非最终测试。BC开发面板两布局轨迹一致。主流程已进入L3对D2 RL，活动句柄4817。

L3已完成12×256 RL；D2开发分依次40.00/42.92/45.00/47.08/48.33/47.50/48.75/49.58/48.75/51.25/53.33/50.83%。按开发集最高分选第11轮（000016），高于BC000015的40.83%，作为L4初始权重；不据开发集选模后的CI宣称独立优势。原始L3结果及选中开发面板已拉回。主进程4817已实际开始L4的1024局示范采集，当前总目标未完成。

L4 BC000017完成1024示范/40507决策/8000更新，候选池21种全部有入组覆盖。D2开发221000得分59.17% [53.33,64.58]，两布局轨迹一致；尚非最终独立证据。主流程已进入L4对D2 RL，相关状态和开发面板已拉回本机。

L4全部12轮完成：D2开发57.08/57.08/55.00/54.17/54.58/57.50/58.75/59.17/59.58/59.17/57.92/59.58%。并列取最早，选000018第9轮（59.58%）向L5迁移；BC000017为59.17%，不声称RL增益显著。主进程4817已进入L5示范采集，新增5张角色天赋；状态/开发面板已拉回。


自动收尾 `finalize_curriculum` 已在56启动（stream59571），等待主流程4817完成后调用独立复核，再对三个冻结候选各做240场景双席位的逐卡审计。检查30张/最多两份、26原牌见过、动作使用属于合法机会、候选SHA与源码指纹一致；不将“每张都出过”设为门槛。输出 `finalization.json`，保留实际强度门槛结果，不自动声称目标达成。测试验证未完成/失败课程不会提前审计，错误牌组与缺失卡覆盖被拒绝。最终仍需拉回产物和审阅完整报告。


L5 BC000019完成（采集+训练1519秒），26种阶段候选均实际入组，最少1161份。D2开发231000：51.25% [45.0,57.5]，480局、无平局、40局回合上限裁决，双布局240对轨迹完全一致；不足以证明D2优势。与L4使用不同卡池和种子，不能直接将分差归因为某一张卡。主流程已进入L5 RL，控制状态及BC开发原始面板已拉回；自动收尾59571继续等待。


L5 RL000020前两轮D2开发54.17%/55.42%。第1轮239/240对布局轨迹相同（胜负全部相同），定位到index2/side1第4回合第37步：两个占星调和动作的分数约44.78521，最大跨布局误差7.63e-6，乘1e5舍入后引发54/56两个索引并列边界变化。物理视图、动作身份、支付数组完全相同；属于float32近似并列数值敏感性。诊断原始 `l5/layout_case_1.json` 已拉回，工具 `layout_case.py`。未在训练途中改策略或放宽门槛。第2轮240/240轨迹一致；最终冻结候选仍需独立一致性验收，不能把当前非最终轮的一次差异忽略为已解决。


L5 RL000020已完成6/12轮，D2开发54.17/55.42/56.67/56.25/58.75/60.00%。第6轮288/480胜、34局超时裁决、240/240布局轨迹一致；开发CI[55,65]，不代替独立终评。当前最佳面板及前六轮记录已拉回，主进程4817继续后六轮。


L5全部12轮完成：D2开发54.17/55.42/56.67/56.25/58.75/60.00/59.58/56.67/60.42/62.92/65.00/59.58%。按开发集最高分选000020第11轮向L6迁移，SHA `58d24866fd45eb4bd6ba028d93cd61ee1219cb2380cbdaeb269d500f8a03a617`。该候选开发240/240布局轨迹一致，CI[59.58,70.42]，非独立测试结论。主进程4817已开始L6完整原26牌（另保留2demo）的示范采集。根状态、L5完整RL记录、选中开发面板、L6初始状态均已拉回。用户已了解阶段/轮次/开发集含义，后续以阶段结果和异常为主汇报。


L6 BC000021已完成1024局示范并进入8000步训练，完整28候选类型均实际入组，原26种无遗漏，最少1022份；当前步数已过4500。已拉回热启动阶段状态用于覆盖核查，主进程4817继续运行。


用户新增观察要求：寻找“开局使用L6牌奠定胜势”的实际例子，并检查D2是否未识别其价值。工具 `early_l6.py` 已实现：240场景双席位，记录第一/第二回合三种L6牌的使用、D2同状态选择和完整动作/血量/骰子轨迹；每种取最多3个早期获胜且D2不同意的案例，32组同随机种子的单动作替换对照（后续同NN对D2，允许替代分支稍后再出同牌）。这属于探索性选例，不能将条件胜率当总体强度或宣称所有牌都有价值。56已启动L6 RL000022第1轮案例基线，stream63740，输出 `artifacts/early_l6_initial`，4 workers；主训练4817不受改动。最终冻结模型还需同口径审计，并对比L6 BC起点，区分监督已有策略与RL增量。

L6 BC000021已完成：D2开发57.92% [52.08,63.75]，240/240布局轨迹一致；已进入RL000022，首轮57.92%。BC/开发及首轮状态已拉回。


最终早期策略审计已通过 `early_l6_followup` 排队到56（stream51800，8 workers），等待 `finalization.json` 完成后按同240场景/321000种子审计L6 BC与全部三个最终候选。初始RL第1轮搜索480局完成：乘胜/以攻/以逸前两回合使用7/8/3局，首回合5/6/2局，早期出牌获胜5/7/3局；以逸3例D2均选其他动作。index2 side0与index50 side1均首回合以逸，最后第7回合天星剩3血获胜。控制结果已拉回，32组配对分支仍在63740运行，尚不能下因果结论。


中断记录：L6 RL000022完成前9轮，第10轮更新发生CUDA unknown error，主stream4817终止exit1，自动收尾59571及51800退出。用户确认同时使用Lightroom修图，目前保持GPU训练停止，不降低总训练/验收预算、不自动重启。下一步验证最新恢复点迭代9并精确接第10轮；课程主入口无resume，恢复实现仍待补。GPU低负载查询成功，CUDA探针启动被用户打断且未确认结果。


用户结束占用后明确恢复：新入口 `recover_curriculum` 保留失败记录/旧权重/原课程归档，验证CUDA基础计算与latest迭代9，恢复原优化器和随机状态后从第10轮接续。主stream13637已运行，新的自动验收67126、最终早期L6审计16612也已重新挂接。11项恢复/验收测试通过。新增 `recovery_1/failed_master.json`、恢复源码归档及后续合并轮次索引，不重写旧000022失败记录；算法/预算/种子不变。
