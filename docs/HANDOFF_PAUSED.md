# 暂停交接

更新时间：2026-09-22。

## 授权状态

56 当前**允许使用**。这是一项持续状态：

- 用户明确授权后，保持允许，直到用户明确取消；
- 用户明确取消后，保持禁止，直到用户明确重新授权；
- 当前授权包含远端探查、同步、训练、评估和其他高性能操作。

当前没有活动远端任务。

## 当前目标

在保持零样本/少样本泛化能力的基础上，稳定战胜 F1-D2。目标尚未完成。

正式完成标准：多训练种子、独立测试 seed、配对场景聚类 95%CI 下界高于 50%，同时 heldout 规则变体无明显崩溃。

## 已确认结论

- executed 模仿目标是语义路线唯一稳定阳性改进；RL16 达到原生约 42–45%、heldout 约 40–43%，但未过完成线。
- 数据和步数翻倍、d128×2L、DAgger、RL 预算翻倍、后果残差、去锚、事后 value head、三种推理时搜索均未建立可复现收益。
- 从零 ExIt 的 z 目标证明 value 学习机制可工作，但 g225 只有 argmax 8/220、MCTS16 10/220，无法接近 RL16。
- 无锚 RL16 热启动 ExIt 明确退化：ep256 argmax 51/220、MCTS16 33/220。
- 锚定 beta=0.3 仍无正向证据：
  - argmax：75/220，对同种子基线 83/220；
  - MCTS16：66/220，对同种子基线 64/220。
- beta=1.0 run 只完成 12 局、797 条 transition 和 32 次训练更新，不能据此判断锚定强度是否有效。
- RL16→AZ 参数移植未保留语义网络行为。4 局 823 个同状态决策中，普通决策 argmax 一致率 71%，最大概率误差 0.608。
- 端到端 ExIt 已暂停。当前主路线为终局反事实重骰学习，再恢复语义 DMC；搜索必须先证明独立 teacher gain。
- 修正隐藏信息后的反事实教师完成 32 个先手根资格评估，平均根配对收益 -0.0104，95% 区间 [-0.0933, +0.0725]。预算耗尽后仍跨零，当前教师暂停，未训练 reroll adapter。

完整数字与工程过程见 [ExIt 报告](5_history/exit_pilot_20260918.md)、[F1-D2 总结](5_history/d2_campaign_20260917.md)及各专项报告。

## 56 最后已知状态

最近一次检查显示没有活动 Python 训练或评估进程，CPU/GPU 近空闲。

run `202609200935_000084_exit_ws_anchor_b1`：

- 配置：`configs/az/exit_ws_anchor.toml`
- 单因素：`anchor_beta=1.0`，`lr=3e-4`
- 首个有效更新后 checkpoint：`ckpt_2431.pt`
- 远端 metadata 已标记为停止；本地拉取副本保留了停止前的陈旧状态
- 没有对应的 beta=1.0 配对评估目录
- checkpoint 状态为 `step=2431`、`total_episodes=12`、`total_transitions=797`、`train_steps=32`

本地结构清理改变了源码指纹。该 run 只作为历史产物保存，不进入新路线评估。

## 本地工作区

- 当前发布基线为 `v0.3.2`，分支 `dev`，本地提交尚未 push。
- 2026-09-22 已完成：
  - 所有当前 Python 改动 Ruff 格式化；
  - 五个超限生产文件完成职责拆分，300 行门禁恢复；
  - 受影响的 AZ、need_target、greedy 与远端同步测试 106 通过。
  - `v0.3.2` 已通过 Go 引擎测试、训练测试、环境与 Web 后端测试、前端 lint/生产构建和 diff 检查。
- 每回合正常重掷已进入引擎；旧 checkpoint 未在该环境中训练，既有评估结果不能直接代表当前规则强度。
- `ref/genius-invokation` 是独立参考包，其 Python 测试依赖未安装的 `gitcg` 绑定，不属于上述项目验收范围。
- 当前本地源码指纹：`dbb7459104dac9f2cdd1cfe860cdd3b924ab76b18fb68805c2f24f56d879f51f`。
- 禁止使用 `git reset`、`git clean` 或覆盖现有工作区。

## 恢复顺序

1. 若工作区继续变化，重跑与改动相称的本地测试和门禁。
2. 保持提交信息描述最终交付，不描述中间调试过程。
3. 固化公开牌表来源，补全后手重骰根的隐藏骰面后验。
4. 重新设计低方差候选选择，并在全新根状态与验证种子上重新验证配对收益。
5. 教师通过后才训练冻结普通路径的 reroll adapter；随后才恢复分层 replay 的语义 DMC。
