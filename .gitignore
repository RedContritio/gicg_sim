rl_mvp_v3.txt
# .claude/ contents are local-only, EXCEPT the OpenSpec workflow integration
# below which travels with the repo. Use `.claude/*` (not `.claude/`) so git
# still descends into .claude/ to evaluate whitelist patterns.
.claude/*
!.claude/commands/
.claude/commands/*
!.claude/commands/opsx/
!.claude/skills/
.claude/skills/*
!.claude/skills/openspec-apply-change/
!.claude/skills/openspec-archive-change/
!.claude/skills/openspec-explore/
!.claude/skills/openspec-propose/
!.claude/skills/run/
# 其他 agent 工具的本机状态目录，同 .claude/ 一样不入库
.commandcode/
.venv/
__pycache__/
*.pyc
*.o
*.a
*.so
*.dylib

# Go build output dir (cmd/gicg_actor 等 standalone binary,I29 redesign 2026-05-25)
bin/

# Go `go test -c` 输出 (在 gicg_engine/tests 等运行 go test 时偶然 leak,I29 R6 cleanup)
tests.test
*.test

# macOS Finder metadata (auto-created;不该 track)
.DS_Store
tools/cleanse_4_raw.py
data/full_haiku/
docs/3_plans/cards/effect_mechanism_inventory.md

# external reference implementations (do not commit)
ref/

configs/hosts/hosts.toml
