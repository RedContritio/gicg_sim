# s015_bc_smoke: 1k-decision pipeline sanity before committing to 50k run.

run_label = "s015_bc_smoke"

teams = ["测试角色D"]
fix_dice = "none"
max_rounds = 3
card_pool = []
data_dir = "data"

teacher = "F1-D2"
teacher_dice_greedy = true
opponent_mix = ["F1-D2", "F1-D1"]

target_decisions = 1000
max_games = 200

max_actions = 128  # must match PPOConfig.max_actions (bumped from 64 after legal-size audit)
seed = 42

[meta]
paradigm = "bc"
