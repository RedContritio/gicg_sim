# s009: first PPO Stage 1 smoke (random dice, otherwise Stage 0 spec).
#
# Stage 1 variation from Stage 0 = fix_dice dropped, per curriculum
# plan line 272. base="default" already has fix_dice=None so no
# explicit override needed (Stage 0's s008 is the one that writes
# fix_dice = [...] to pin it).
#
# Result (2026-04-25 00:45, wall=118.5s): FAILED.
#   random=0.812 / F1-D1=0.312 / F1-D2=0.000
#   Replay showed policy collapsed to Tune-heavy (Round 1 zero
#   attack). 500 iter + noisy value target meant PPO couldn't
#   recover from initial bias. Triggered the diagnostic chain
#   s010→s011→s012→s013→s014.

base = "default"
run_label = "s009_ppo_stage1_smoke"

n_iterations = 500
eval_every = 25
n_eval_games = 16

[meta]
paradigm = "ppo"
