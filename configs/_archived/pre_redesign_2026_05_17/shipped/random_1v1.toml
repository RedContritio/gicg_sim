# Random 1v1 + domain randomization (D10) — 500g, 5-char pool.
# First run validating per-game team sampling. Superseded by
# shipped_1v1.toml (C1v7 architecture) for substantive training;
# kept for short 500g smokes.

base = "random_1v1"
run_label = "random_1v1_500g"

n_games = 500
n_workers = 4

[mcts]
parallel_rollouts = 4
backend = "go"

[meta]
paradigm = "az"
