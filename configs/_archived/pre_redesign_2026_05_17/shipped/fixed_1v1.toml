# Fixed 1v1 mirror-free (赤蝶 vs 墨客), single-char baseline.
# Kept for reproducibility of pre-domain-randomization runs.

base = "fixed_1v1"
run_label = "fixed_1v1"

n_workers = 4

[mcts]
parallel_rollouts = 4
# Go backend default post-Phase-A validation. See shipped_1v1.toml.
backend = "go"

[meta]
paradigm = "az"
