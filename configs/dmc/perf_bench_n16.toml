# DMC mp benchmark cfg — N=16 actor production scale(9950X3D physical core 上限)。
#
# Extends stage3_b_v_legacy.toml。对照 backlog I24/I25:N=16 fps 36.3 → 37.4 +
# total mem 12.6 GB。total_frames 10000 ~4.5 min wall。

[meta]
extends = "stage3_b_v_legacy.toml"
paradigm = "dmc"
run_label = "dmc_perf_bench_n16"
host = "remote"

[remote]
ssh = "dev@192.168.31.56"
root = "D:/gicg_dev"
os = "windows"
hostname = "DESKTOP-GHJCC7Q"

[pipeline]
num_actors = 16

[paradigm.dmc]
total_frames = 10_000
