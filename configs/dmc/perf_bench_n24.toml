# DMC mp benchmark cfg — N=24 actor oversubscription 复核。
#
# Extends stage3_b_v_legacy.toml。backlog I26 ship 结论:N=24 因 OS CPU oversub
# (16 物理核 vs 27 进程)倒退到 17 fps,**不可用**。本 cfg 仅作 I30 后复核。
# total_frames 5000 减 wall(N=24 慢)。

[meta]
extends = "stage3_b_v_legacy.toml"
paradigm = "dmc"
run_label = "dmc_perf_bench_n24"
host = "remote"

[remote]
ssh = "dev@192.168.31.56"
root = "D:/gicg_dev"
os = "windows"
hostname = "DESKTOP-GHJCC7Q"

[pipeline]
num_actors = 24

[paradigm.dmc]
total_frames = 5_000
