# 有序效果实例与可微调规则编码

用户批准：确定性模拟器使用静态规则定义与动态 buff 列表；同类别按产生顺序结算。
神经网络看到相同的规则与实例状态。零样本作为评估目标，允许微调和增加可复用原语。

本次范围：当前训练池 `v_legacy` / `test_basic` 的已确认效果、观测、五范式采样/训练链路，
以及费用理解辅助训练、旧权重适配入口。新增规则仍须先由引擎实现并通过规则测试。

不把模型对未实现机制的猜测当作模拟器规则，也不以训练 loss 下降替代策略泛化评估。
