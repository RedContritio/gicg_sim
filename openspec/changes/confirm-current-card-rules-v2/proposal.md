# Confirm current card rules v2

用户确认三项边界：乘胜打出后当回合计数、打出本身不计入；佛跳墙仅技能
伤害且回合末过期；蝶印对后台带印角色照常结算并清除。

乘胜实现无需改变。修复佛跳墙的来源过滤与期限、蝶印的后台目标，补真实
出牌/换人/回合末/死亡恢复与规则 IR 验收。保留 v1 冻结文档及指纹，
在本目录创建 v2；默认校验入口切到 v2。范围仍是当前训练牌组。
