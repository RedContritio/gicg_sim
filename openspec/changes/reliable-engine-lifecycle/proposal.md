# Reliable engine lifecycle

修复已复现的复制、恢复、重置和延迟执行故障，建立可复现的模拟状态契约。
用户已授权实施。保留现有工作分支，不修改未跟踪的 docs/HANDOFF.md。

## Scope

- 完整复制 Preparing、Supports、装备占用、待处理动作及其内部 map。
- 从共享 Ruleset 移出可变专属装备状态。
- 快照读取与恢复不消耗源状态 RNG；搜索采样使用显式独立随机流。
- 普通快照与池化快照采用相同语义，增加字段清单防止以后遗漏。
- 修复 after-damage / action frame 延迟效果丢失及重入队列引用失效。
- 验证实际 Go / C API / Python / 搜索消费路径，更新旧随机性契约。

完整正式卡池的规则认证、独立参考引擎对拍仍是后续可靠性工作，不能由本批
生命周期修复的测试结果代替。规则导出缺失等已知问题继续保留在任务清单。
