# 验证记录

日期：2026-09-13。

本机两场景最小样本未覆盖负标签，按协议拒绝拟合；扩大到八场景后通过。
本机 16 局、31 个有标签状态完成真实采集和两步拟合，来源不同于正式诊断模型，
仅用于工具验证。双布局物理视图、原生动作身份、支付、终局标签一致性检查通过。
在线与 batch Q 检查通过；离线布局分析工具实跑通过。

正式诊断：远端 `.56`，`artifacts/terminal_fit_main`，SSH 会话 87883。
16 局采集完成，35 个状态：训练 28、留出 7。
训练标签 +1 共18、-1 共36；留出标签 +1 共4、-1 共9。
全部拟合前在线与 batch Q 最大误差为 0。
400 步隔离拟合完成，SSH会话正常退出；原权重哈希前后相同，诊断副本不进入正式策略池。
训练原布局MSE为0.000306，同状态重排0.9415，留出原布局0.9043，留出重排3.2447。
已标注正负候选的混合状态排序四面板全部正确，但不能代表全部合法动作的选择质量。
数据、隔离权重和四面板结果单独保存，结论见 docs/5_history/terminal_fit.md。

Ruff 格式、未定义名称检查与 OpenSpec 索引检查已通过。
