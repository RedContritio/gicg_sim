# 直接扩展到五角色2v2

2026-09-13用户决定保留角色数值、跳过1v1调平，直接训练2v2；随后明确指出正常超时应分胜负。
2v2配置恢复max_rounds=10，以便data/system/timeout.lua先裁决：存活人数多者胜，同人数首回合后手胜。
之前1v1训练的max_rounds=8是提前截断判平，历史成绩仍按原配置解释，不改历史结果。

## 任务

五角色中每局选四个，双方各2个，无角色重复，队内顺序随机，保留当前两测试行动牌与填充牌。
训练sample_config显式采样后传入env_factory；评估15个无序不相交队伍配对等量覆盖。
每个评估物理场景让模型分别控制两侧，布局随机种子独立；结果含逐队伍配对得分。
这是五角色完整池的2v2任务迁移，不声称留出新卡泛化。

## 首轮

致死超载回归修复改变环境指纹，弃用失败2v2试跑权重；在修复环境重新采集512局D2示范、4000步BC作为初始化与KL锚定，再做纯终局奖励RL。历史1v1权重不改指纹绕过校验。
训练seed140000、8轮×128局、16采样进程、batch16；其余沿用lr1e-5、最多2epoch、ratio clip0.2、KL锚定0.02。
开发seed141000、60场景（每配对4场景）×双侧×2布局，先测初始化基线，再逐轮评估。
按开发最高分/并列最早选候选；最终新seed142000、240场景（每配对16场景）×双侧×4布局，配对比起点。
另seed143000、60场景检查四布局同状态一致性。单列超时裁决局数/胜数及歼灭胜数，避免把10回合裁决当自然击败。
小预算smoke使用140900，仅验证工程链路；不作为正式训练起点。

## 接入

新增configs/dmc/semantic_duo.toml、tools/experiments/semantic_training/teams.py。
修改实验采集/评估以实际使用随机阵容；core.env_factory不修改，旧1v1配置保留原行为与指纹兼容。
RL checkpoint/settings保存实际scenario，拒绝跨不同场景恢复优化器；初始权重迁移仍允许。
对应真实阵容采样、15配对覆盖、静态/动态重排、全局梯度与RL采样测试通过。

## 验收与修复

致死超载延迟切换仅在受击者仍存活时执行，死亡替补选择保留；新增is_char_alive原语及NN可见token315。双方席位1/2/3角色、死亡选择、克隆/快照恢复、单次换人事件均回归。
最终独立评估要求对D1的场景聚类95%区间下界超过50%，同时报告对D2结果；若劣于D2，按阵容与行为诊断原因并提出方案。监督起点与RL配对比较，不能用BC成绩冒充RL改善。

首轮已完成：RL候选第7轮对D1 76.67%（95%72.71–80.63）、对D2 55.00%（51.04–58.96），D1验收通过；RL相对BC增量不显著。4757状态重排一致性100%。详docs/5_history/semantic_duo.md；本轮任务均已结束。
