# 多布局DMC从3k续训至30k（2026-09-12，运行前固定）

用户授权延长训练，并加入“技能滥用者”评估对手。
41/42/43均从shuffle_rl_v15预算末完整检查点分支续训；原检查点保留不覆盖。
恢复优化器、经验池、历史对手和RNG。只增加累计frames预算，网络、奖励、
学习率0.0003、epsilon0.2、经验池10000、训练对手混合及CPU单线程不变。
三个种子目标均为累计30000 agent frames，整局结束可小幅超出。
阶段6000、12000、20000、30000保存快照并用固定开发集评估；正常波动不提前停止。
仅非法动作、非终局截断、非有限训练数值等完整性错误中止并诊断。

评估对手：uniform random、F1-D2、技能滥用者；第三对手原始费用优先级见v16 design。
开发master109000，32scenario×换边=每对手64局，起点3k及四阶段都评估。
最终master110000，64scenario×换边=每对手128局；仅比较3k与30k。
每scenario一个独立布局，各模型、换边、对手共享scenario；报告逐局及配对区间。
最终集不用于选择检查点，默认末尾30k。主判据沿用对随机三种子score>0.5，
F1-D2相对3k平均提高>=0.05且最多一个种子退步。第三对手先报告全体分数和变化，
不因看到结果临时加入合取门槛。报告各阶段走势；达到30k也不自动追加100k。
当前能力边界保持1v1，不启动5070Ti迁移。

生产来源8bc1dce77cd90f8eaf7f71be117bdf532df1da036bd8f62123b79521bf1a8dc5保持不变。
评估代码增量冻结为skill-abuser-eval-v16；训练v15权重原兼容校验不变。
初始/阶段开发总计2880局，起点/终点最终共2304局，合计5184局评估。
