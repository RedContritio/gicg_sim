# 显式暂停执行帧：第一阶段

付费卡牌等待选目标时，保存一个可验证、可复制、可序列化的执行帧，并向决策网络暴露程序入口和可见局部参数。目标选择不得重新付费或重复出牌。

这是完整执行程序迁移的第一阶段。当前训练通常枚举卡牌与目标的联合动作，因此这条兼容选目标路径并非训练中主要的暂停来源。伤害致死后的嵌套 Go/DSL 调用仍由原有重放机制恢复；新增的“不完整”标记只说明表达边界，不能消除信息缺失。

不启动远端长训，不复用旧权重、旧数据，不覆盖 v6/v7/v8 源码清单。
