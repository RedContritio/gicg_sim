# v9 第一阶段验收 — 2026-09-12

| 验收 | 结果 |
| --- | --- |
| Go 引擎、解释器、record、MCTS | 通过 |
| Go actor、DMC socket、共享内存 | 允许本机系统接口后通过 |
| Python training + env（排除已知无上限 minimax 用例） | 1227 passed，6 skipped，22 个既有 JIT 弃用 warning，45.50 秒 |
| 五范式完整 smoke：真实训练、保存、断点续训 | 10 passed，614.18 秒 |
| 目标帧、legacy target、真实强制换人竞态检查 | 通过 |
| 独立运行时恢复、非法检查点、实际支付不重复专项 | 通过 |
| 源码基线工具单元测试 | 5 passed |
| 本轮 Python 格式、Go 格式、OpenSpec 索引 | 通过 |

初次受限沙箱运行出现端口、共享内存和进程指标权限失败；在允许这些本机接口的环境重跑后通过。详见 [验证命令及范围](verification.md)。完整 smoke 期间生产源码保持不变，临时训练产物全部新建。

新增能力：卡牌目标输入的数据执行帧、帧消费校验、版本 2 检查点、两类 NN 实体、私有费用条件遮罩，以及明确的重放路径不完整标记。v8 网络权重维度/来源指纹不兼容，不迁移或复用；游戏检查点 v1 也不兼容。

剩余：嵌套伤害、DSL 局部变量/返回栈、延迟队列和回合阶段仍使用根快照重放，没有完整公开剩余程序。当前池通常使用卡牌与目标联合动作，因此本轮兼容目标选择帧的覆盖不能替代主要死亡暂停路径的后续迁移。本报告不是完整规则认证，也不是策略学会游戏或零样本迁移的证据。未启动远端长训。
