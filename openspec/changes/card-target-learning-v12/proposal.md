# 卡牌目标可见性及条件学习验证

v11 后续状态对审计发现，目标联合动作已由引擎枚举，但 C API/Go actor 丢失了
目标字段，共享动作头也只取卡牌规则与付款，导致不同目标不可区分。
修复端到端目标表达，并用合法可达、角色和双方均衡的状态验证学习与消融。

广泛回归另发现超载 DSL/builtin/IR 名称不一致造成随机对局崩溃；使用固定动作
序列定位并最小修复。阶段 3 尚未完成全部辅助任务及短程 RL。
