# 小范围训练环境准备验收 — 2026-09-12

**结论：当前 DMC 默认配置、readiness_base（1v1）、readiness_tactics（2v2）可以进入
下一阶段小模型可学性验证。** 本轮未启动正式小范围学习实验或远端长训。

| 检查 | 结果 |
| --- | --- |
| Go 引擎/解释器/record/MCTS/actor | 通过 |
| Python training/env/experiments/eval/cards 广泛回归 | 1323 passed，6 skipped，49.23 秒，22 个既有 JIT warning |
| 后续新增/补强的环境 gate、真实引擎平局和终局标签专项 | 14 passed；gate 防旧绿色报告补强后另 3 passed |
| DMC 完整 smoke（临时目录训练、保存、续训） | 1 passed，345.11 秒；这是入口回归，不是策略能力实验 |
| 来源、连续暂停、目标帧 race 检查 | 通过 |
| Go 随机序列/独立实例参考模型 | 12/24 种子通过，见 coverage |
| 初训配置无学习预检 | 32 场、1610 次输入、16 次强制换人，全部终局且分支一致 |
| 原 DMC 默认配置附加无学习预检 | 8 场、325 次输入，通过 |
| 默认/初训/留出效果依赖审计 | 4 配置、184 个绑定、0 问题 |
| 小模型真实前向 | 276672 参数；合法输出有限，预检前后参数完全一致 |
| 格式与 OpenSpec 索引 | 通过 |

广泛回归沿用显式排除 `test_budget_uncapped_equals_old_const` 的限制，未把无上限搜索
用例计为通过。广泛回归后只修改了预检报告失败处理及补充测试，已做相应定向复验。
本轮网络尺寸和词表没有变化，未重复五范式完整 smoke；广泛回归包含五范式默认 smoke，
完整保存恢复测试聚焦发生采样/标签行为变化的 DMC。

本轮发现并修复：公开暂停来源缺失；ExportView 状态列表的 map 顺序不稳定；文件评估
将平局编码 2 当失败；DMC 将未终局截断回填成平局。严格标签回归暴露的旧测试夹具
用无 winner 字段的列表冒充 EpisodeRecord，已改为明确终局记录，没有放宽生产验证。

新增预检入口会在失败时保存种子、配置、策略及准确输入前缀，并使原绿色报告失效。
实际报告见 repeat-verification.json，规则加载/效果绑定见 effect-inventory.json。
这些数据用于环境验收，不作为策略得分或训练样本。

边界：三角色多选暂停、完整剩余程序表达、任意私有 DSL 控制流、新卡正确性、远端并行
吞吐与模型泛化不在此次就绪结论内。完整帧化仍按证据推进，不是开训前置要求。
旧来源指纹的权重/采样不复用。后续从头按路线阶段 3 做辅助任务和短局面学习，再进入
已准备配置的 3 种子小范围 RL；命令见 verification.md。
