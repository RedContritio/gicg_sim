# 提交前职责拆分与异步采集种子修复

用户要求修复异步采集错误，并正常重构本次涉及的历史超限文件。

异步采集统计误用了仅在 serial 分支定义的 `seeds`。改为记录
`EpisodeRecord.scenario_seed`，不按汇总端的到达顺序重建或伪造种子。
本次不扩展异步排列随机化，也不改变 serial 的种子、采样或学习行为。

11 个超限文件按职责拆分，保留公开类、方法与函数入口；不绕过提交钩子。
涉及 C 状态/观测接口、治疗与能量、动态历史观测、测试辅助与基准、
环境状态操作、NN 观测缓存、DMC 批量前向和请求观测解码。

生产文件路径/字节变更会形成新的严格来源指纹。旧检查点继续绑定 v17 原始代码；
不修改旧 manifest，不将旧检查点伪装为新来源，不自动迁移或开始训练。
