# v6 规则边界

沿用 [v5 已确认规则](../effect-audit-v5/rules.md)，本次不改变引擎或卡牌结算。
用户本轮再次确认：召唤物按双方宣告结束的先后结算，同方内部按产生顺序；
其他 buff 跨双方按产生顺序结算，不按所属方分组。既定阶段与类别优先级保持不变。
当前实现与此次确认一致，无需改动引擎或重采本轮实验。
旧权重、数据集与旧训练结论均已废弃，不能通过适配入口复活。
