# 覆盖范围

- 五范式文件模型入口、BC 与费用探针 NPZ、CFR reservoir 均使用来源校验。
- 未标记、旧 epoch、不同源码指纹文件拒绝；网络加载前验证。
- 旧输入形状适配拒绝；干净的原语词表扩行保留。
- 真实训练、留出组合、两档微调、遗忘与同预算从零对照均有 3 种子数据。
- 16 组评估局面 × 两侧 × 两种对手；逐场结果公开，最终评估无步数截断。
- 费用学习与新原语表示扩展独立报告，不代替整局策略效果。
